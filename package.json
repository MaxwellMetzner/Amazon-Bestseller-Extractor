{
  "name": "amazon-bestseller-extractor",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript build tooling for the Amazon BSR Chrome extension.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "watch": "tsc --watch -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "typescript": "^6.0.2"
  }
}
